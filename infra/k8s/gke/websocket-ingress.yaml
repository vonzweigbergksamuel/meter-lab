apiVersion: v1
kind: Service
metadata:
  name: websocket-service
  namespace: meter-lab
  labels:
    app: meter-lab
spec:
  type: ClusterIP
  selector:
    io.kompose.service: backend
  ports:
  - name: http
    port: 5071
    targetPort: 5071
    protocol: TCP
