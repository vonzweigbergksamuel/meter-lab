apiVersion: v1
kind: Service
metadata:
  name: websocket-service
  namespace: meter-lab
  labels:
    app: meter-lab
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: backend-websocket
  ports:
  - name: websocket
    port: 5071
    targetPort: 5071
    protocol: TCP
