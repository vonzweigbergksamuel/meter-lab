apiVersion: v1
kind: Service
metadata:
  name: meter-lab-auth-service
  labels:
    app: meter-lab
spec:
  type: ClusterIP
  selector:
    io.kompose.service: auth
  ports:
  - name: http
    port: 80
    targetPort: 5090
    protocol: TCP