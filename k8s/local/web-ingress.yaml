apiVersion: v1
kind: Service
metadata:
  name: meter-lab-loadbalancer
  labels:
    app: meter-lab
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: meter-lab
  ports:
  - name: http
    port: 80
    targetPort: 3002
    protocol: TCP
