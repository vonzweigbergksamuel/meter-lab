apiVersion: v1
kind: Service
metadata:
  name: meter-lab-loadbalancer
  labels:
    app: meter-lab
spec:
  type: LoadBalancer
  selector:
    io.kompose.service: backend
  ports:
  - name: http
    port: 80
    targetPort: 5070
    protocol: TCP
